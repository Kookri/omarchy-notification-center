{
  "schemaVersion": 1,
  "id": "jankeesvw.notification-center",
  "name": "Notification Center",
  "version": "1.0.0",
  "author": "Jankees van Woezik",
  "description": "Every notification you were sent, kept, and readable again",
  "homepage": "https://github.com/jankeesvw/omarchy-notification-center",
  "license": "MIT",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "Notification Center",
    "description": "Every notification you were sent, kept, and readable again",
    "category": "System",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "panelWidth": 420,
      "listHeight": 480,
      "badge": "Dot",
      "keepDays": 30,
      "maxItems": 1000,
      "clickAction": "Auto",
      "showBody": true
    },
    "schema": [
      {
        "key": "badge",
        "type": "enum",
        "label": "Mark what you have not read",
        "options": [
          "Dot",
          "Count",
          "None"
        ],
        "defaultValue": "Dot",
        "description": "What the bell says about notifications that arrived since you last opened the center. A dot says there are some, which is the question you actually ask of a bar; a count says how many, and turns the bell into something you read rather than glance at."
      },
      {
        "key": "keepDays",
        "type": "integer",
        "label": "Keep notifications for",
        "min": 1,
        "max": 365,
        "step": 1,
        "defaultValue": 30,
        "description": "Days. Past this a notification is deleted along with the icon kept for it. Whichever of this and the count below is reached first wins."
      },
      {
        "key": "maxItems",
        "type": "integer",
        "label": "Keep at most",
        "min": 50,
        "max": 10000,
        "step": 50,
        "defaultValue": 1000,
        "description": "Notifications. A ceiling on the archive regardless of age, so a chatty week cannot grow it without limit. Roughly a kilobyte each, plus whatever icons come with them."
      },
      {
        "key": "clickAction",
        "type": "enum",
        "label": "Clicking a notification",
        "options": [
          "Auto",
          "Focus the app",
          "Nothing"
        ],
        "defaultValue": "Auto",
        "description": "Auto does what the notification itself asked for when it said anything, so a screenshot toast still opens its screenshot a week later, and falls back to focusing the app that sent it. Focus the app never runs a stored command, which is the cautious reading of an old notification."
      },
      {
        "key": "showBody",
        "type": "boolean",
        "label": "Show the message text",
        "defaultValue": true,
        "description": "Off leaves the sender and the subject line only, which is the version to run on a screen other people can see."
      },
      {
        "key": "panelWidth",
        "type": "integer",
        "label": "Panel width",
        "min": 320,
        "max": 900,
        "step": 20,
        "defaultValue": 420,
        "description": "In the shell's spacing units. Anything wider than the screen is trimmed to fit."
      },
      {
        "key": "listHeight",
        "type": "integer",
        "label": "List height",
        "min": 200,
        "max": 1200,
        "step": 20,
        "defaultValue": 480,
        "description": "How tall the list is allowed to grow before it starts scrolling. A short list that scrolls beats a tall one that runs off the screen."
      }
    ]
  }
}
